# Ablation Process in this work

This section documents the steps that are taken in `ablation.`

`python3 -m ablation.movie.run`

One can also specify arguments with it. But the intent of running the ablations are 